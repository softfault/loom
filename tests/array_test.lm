[Main]
main = () int
    # 1. 字面量创建
    arr = [1, 2, 3]
    print("Initial len: " + arr.len())

    # 2. 修改数组 (push 是 native method)
    arr.push(4)
    arr.push(5)
    
    # 3. 索引访问 (需要在 Parser/Interpreter 实现 IndexExpr)
    # 如果还没实现 arr[0]，可以先跳过，或者实现一个 native get(i)
    print("First element: " + arr[0]) 

    print("--- Testing For Loop ---")

    # 4. 循环遍历 (真·For循环)
    sum = 0
    
    # 支持 for item in arr
    for item in arr
        sum = sum + item
        
        # 测试 continue
        if item == 2
            print("Skipping 2")
            continue
            
        print("For Looping... item=" + item + ", sum=" + sum)
        
    print("Final Sum: " + sum)

    return 0