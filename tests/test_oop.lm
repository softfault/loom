# tests/test_oop.lm
use .animal_lib

# 在这里定义一个 default_sound，测试是否会干扰父类
default_sound = "Main Sound (Should not appear)"

# 继承外部模块的类
[Dog : animal_lib.Animal]
breed: str = "Shiba"

[main()]
    d = Dog()
    d.name = "Hachiko"
    
    # 调用继承自 Animal 的方法
    # 预期输出：Hachiko says: Generic Sound
    d.make_sound()